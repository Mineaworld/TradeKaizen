export default function PublicLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  // This component is called PublicLayout. It takes a prop called 'children', which represents any React elements passed to it.
  // The function returns the 'children' prop, meaning it will render whatever is passed inside this layout.
  return children;
}
